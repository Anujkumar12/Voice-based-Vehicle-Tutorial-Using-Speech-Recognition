Voice-based-Vehicle-Tutorial-Using-Speech-Recognition
--to start the project just simply run it 
--then on console you will see two basic operations
    -- Train
    -- Play
--Train is basically for new user ,so that system builds the model for the respective users voice i.e live testing.
--Play is for old users,on procedding with play you need to speak name of the type of the vechile
--then it will show the respective image corresponding to that image.and the image will close automatically after seconds.
this is the overall basic working of the project.   